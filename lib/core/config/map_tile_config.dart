/// Legacy placeholder kept for compatibility with older project updates.
/// Google Maps SDK now provides the base map tiles directly.
abstract final class MapTileConfig {
  static const userAgentPackageName = 'com.mndrs.edible';
  static bool get usesWikimediaTiles => false;
  static bool get usesDefaultOpenStreetMapTiles => false;
}
